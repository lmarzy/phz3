# phz3

A framework-free, single-page portfolio built with plain HTML and CSS.

## Local preview

Open `index.html` directly, or run any simple static file server from the repository root.

## Netlify

Connect this repository to Netlify. No build command is required and the publish directory is `.`. These settings are also included in `netlify.toml`.
